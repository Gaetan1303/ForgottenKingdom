ForgottenKingdom - validation V2
Date: 2026-09-03
Target runtime: Godot 4.4 stable (user runtime)

Corrections driven by actual user logs:
1. pnj_daily_planner_service.gd
   - class_name moved before const declarations (Godot 4.4 parser compatibility)
   - explicit StatDefs preload preserved after class_name
2. pnj_generator.gd
   - class_name moved before const declarations
   - explicit StatDefs and PnjDailyPlannerService preloads preserved after class_name
3. clan_economy_service.gd
   - get(...) renamed get_resource(...) to avoid overriding Object.get(StringName)
4. clan_manager.gd
   - explicit Dictionary / RefCounted types on the three inference failures reported by Godot
5. test_domain_services.gd
   - ClanEconomyService.can_instantiate() guard added to avoid false DOMAIN_SERVICES_OK after failed instantiation
6. test_compile_smoke.gd
   - new compile smoke test preloading critical autoloads, PNJ services, domain services and Slide 2

Static validation executed before packaging:
- all preload() targets exist
- class_name declaration order checked across all GDScript files
- class_name uniqueness checked
- global_script_class_cache.cfg contains every class_name/path
- no custom func get(...) overriding Object.get remains
- all .tscn res:// external resources exist
- all .import sidecar source files exist
- JSON files parse successfully
- YAML files parse successfully
- UI/scenes diff against previous delivered ZIP: 0 modified UI files
- ZIP integrity checked with unzip -t after packaging

Important limitation:
The current OpenAI container has no executable Godot binary and cannot download one through its runtime network.
Therefore this package is NOT claimed as runtime-tested here. Run the included tests with the user's Godot 4.4:
  godot --headless --path . -s scripts/tests/test_compile_smoke.gd
  godot --headless --path . -s scripts/tests/test_domain_services.gd
Expected:
  COMPILE_SMOKE_OK
  DOMAIN_SERVICES_OK
